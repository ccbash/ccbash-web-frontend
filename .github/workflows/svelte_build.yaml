name: Svelte Build

on: push

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v2

      - name: setup node
        uses: actions/setup-node@v1
        with:
          node-version: '12.x'

      - name: caching npm & node modules
        uses: actions/cache@v2
        with:
          path: |
            .cache
            package-lock.json
            **/node_modules
          key: ${{ runner.os }}-build-ccb-${{ hashFiles('package.json') }}
          restore-keys: |
            ${{ runner.os }}-build-ccb-${{ hashFiles('package.json') }}
            ${{ runner.os }}-build-
            ${{ runner.os }}-

      - name: dependencies
        run: npm install

      # - name: linting
      #   run: npm run lint

      - name: building site
        run: npm run build
